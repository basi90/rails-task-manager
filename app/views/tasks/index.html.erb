<h1>My tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li>
      <%= link_to task.title, task_path(task) %>
      - <%= task.details %>
      (<%= task.completed ? "Completed" : "Incomplete" %>)
    </li>
  <%end  %>
</ul>

<h1>My tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li>
      <%= task.title %>
      - <%= task.details %>
      (<%= task.completed ? "Completed" : "Incomplete" %>)
    </li>
  <%end  %>
</ul>
